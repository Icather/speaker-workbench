# 更新日志

[English](CHANGELOG.md) · [中文](CHANGELOG.zh-CN.md)

这里记录所有值得记的变更。格式遵循 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)；版本号遵循 [SemVer](https://semver.org/)。

## [Unreleased]

### 新增

- **所有面向用户的文档都有了中文版** —— `CONTRIBUTING.zh-CN.md`、`SECURITY.zh-CN.md`、`CHANGELOG.zh-CN.md`、`docs/METHOD.zh-CN.md`、`docs/FIELD-NOTES.zh-CN.md`，与已有的 `README.zh-CN.md` 凑齐。每一对都在顶部带一行语言切换，且中文文档内部的每一个文档间链接都指向对应的中文版。
- **`AGENTS.md`** —— 给编码 agent 的操作上下文：确切的命令、那个会悄悄把真实录音暂存进提交的被跟踪路径陷阱、测试套件强制的"断言必须与数据无关"规矩，以及 `_path()` 的四份拷贝。刻意只保留英文 —— agent 找的就是这个固定文件名，翻译一份出来永远不会被读到。

### 变更

- CONTRIBUTING 的改动对照表现在列出**全部**成对文档，而不只是两份 README。

### 修复

- **在多数窗口宽度下，页面底部够不着。** `main` 用的是 `height:calc(100vh - 45px)` —— 45px 是对 header 高度的**硬编码猜测**。但 header 是 `flex-wrap` 的，真实高度随窗口变化：实测 **1600px 宽时 46px、1280 时 84px、900 时 85px、760 时 123px**。只要它超过 45px，`main` 就溢出视口，而 `body{overflow:hidden}` 让多出来的部分**永远够不着** —— 滚动条也救不回来。修法：让 `body` 变成纵向 flex 容器（`header` 拿自然高度，`main` 吃掉剩下的），把那套 `100vh` 算术删掉。五个宽度全部复验：`main` 的底边现在正好落在视口高度上。这个 bug 能藏这么久，是因为 1600px 下那个猜测**碰巧是对的**。
- **右栏列表无法滚动** —— 它会一直长到超出视口，然后被 `body{overflow:hidden}` 裁掉，于是一份较长的语料只能看到一个显示不完、又滚不动的列表。成因：`main.active` 是 grid，而它的隐式行高由内容决定；grid/flex 项默认 `min-height:auto`，所以 `.cliplist` 的 `flex:1` 只是把它父级一起顶高，而不是在父级内部滚动。修法：`grid-template-rows:minmax(0,1fr)`，并给 `.stage`、`.cliplist`、`.pane`、`aside` 补 `min-height:0`。**两个测试套件都抓不到这一类 bug** —— jsdom 不做布局计算，这正是 CONTRIBUTING 里对界面改动的那条警告。

## [0.1.0] — 2026-09-25

首个公开版本。从一个内部工具里抽出 —— 那个工具当时正被用来在一份 10 份录音、约 7 小时的远场语料上确认说话人身份。

> 这里的一切在发布**之前**都过了一遍隐私检查：demo 语料是合成的，每一张图都由它生成，
> 而那一个曾经取自真实数据的资产被抓住并替换掉了 —— 见 *修复*。

### 新增

**工作台**（`index.html`，单文件，零运行时依赖）

- **认人模式** —— 把一个声纹组在所有录音里的片段串起来连播，1.6 秒前情，单段上限 15 秒
- **校对模式** —— 逐句改说话人，与自动分组分开存储
- **对照模式** —— 把原始分离器编号并排放在分组旁边，不一致处高亮，另有一张双向混淆矩阵
- 全键盘操作、撤销/重做（150 步）、搜索（文本 / 组 / 姓名）、合并组、单段循环、0.75×–1.5× 变速
- 姓名框支持模糊 + 拼音匹配与身份标签（`名字_标签1_标签2…`）
- 双层波形：全曲概览 + ±25 秒放大

**声纹库 —— 在线登记**（`tools/sync.py:build_enrollment`）

- 已命名的组被折叠成质心声纹，存进 `speakers.json`
- 未命名的组与这个库比对后得到建议名，一个按键即可采纳
- **在真实远场音频上实测出的两档阈值**：同录音 `hi = 0.83`，跨录音 `hi = 0.85`，且 1–2 段的组无论得分多高都只给"弱提示"

**互操作**

- 导出：RTTM（10 列）、SRT、VTT（带 `<v name>` 标签）、CSV（带 BOM，Excel 友好）、TXT、Markdown、`voices.json`
- 导入：RTTM（喂给对照模式）、`voices.json`
- 范围可按单个录音或整个语料选择，带实时预览

**管线**（`tools/sync.py`、`tools/serve.py`）

- 路径由配置驱动：`config.json` → `VOICE_DECK_*` 环境变量 → 仓库内 demo 默认值
- 本地服务支持 HTTP `Range`（206），所以拖动音频进度是瞬时的
- 自动发现新增的录音/转写，增量刷新
- 多扩展名输入：`.m4a .mp3 .wav .flac .ogg .aac .opus .wma`

**工具**

- `tools/make_demo.py` —— 完全合成的 demo 语料；共振峰合成语音（纯标准库）、虚构人物、编造的转写。生成耗时约 1.6 秒。
- `tools/calibrate.py` —— 在任意语料上复现那四个阈值分布
- `tools/inspect_embs.py` —— 在同步之前校验产物格式，并能定位 `tag|start` 键不匹配
- 两个与数据无关的测试套件（逻辑跑在 `vm` 沙箱里，另有经 jsdom 的真 DOM 套件）接入 CI；检查数量随数据集变化

**文档** —— 写于发布前的那一轮整理，起因是读了邻近项目（`diarize`、`WhisperX`、`pyannote.audio`、`audino`）的 README，看它们哪里做对了

- `docs/ui.svg` —— 一张带标注的工作台布局示意图，让项目有一张视觉图，却不必附带截图（也就不必附带任何人的录音）
- `docs/METHOD.zh-CN.md` 与 `docs/FIELD-NOTES.zh-CN.md` —— 方法，以及支撑那些阈值的实测现场笔记
- README：与 `pyannote` / `diarize` / `WhisperX` / `GECKO` / `audino` 的能力对照表；四步 *它是怎么工作的*；*什么情况下该用别的*；一段简短 roadmap；运行时依赖的许可证；内联的环境要求
- CONTRIBUTING：一张 *改了 X 也要改 Y* 的改动对照表；两个测试套件各自的分工**以及它们都抓不到什么**；新增一种导出格式的完整示例；一份完成定义清单；隐私类 bug 的上报路径

### 变更

- 示意图在 GitHub 深色主题下可读了 —— 中灰底色加一段 `prefers-color-scheme` 覆盖。原来底色用的是 `#3a3a3a` 的文字，在 `#0d1117` 上基本看不见
- `tools/calibrate.py` 在自带 demo 上不再显得可信：它会打印一段醒目的警告，因为合成声纹会让同人相似度跑到 ≈0.97、异人 ≈0.16，而这两个数在真实音频上都不会出现
- **demo 语料变大了，体积却小了 5 倍。** 4 份录音 / 77 句 / 12 组 / 6 个说话人，以 32 kbps mp3 提交（662 KB），而不是 wav（3.1 MB）。`sync.py` 本来就会在 `audio/<tag>.mp3` 存在时跳过转码，所以克隆下来仍然不需要 ffmpeg。多加的材料是有意义的：只有 5 个短组时，没有任何一组能拿到 `A` 级置信度徽章，"A 级"筛选是空的，整个 demo 看起来像坏了。现在它显示 A2 / B3 / C+5 / C2，以及建议名的两档置信度（1 条 💡 加 4 条弱 `?` 提示）。
- 组的碎片现在共享其父组的音色，所以合并它们这件事，听的人能真的用耳朵验证
- `tools/sync.py` 现在会在覆盖被 git 跟踪的 demo 资产（`audio/`、`data.js`）之前发出警告 —— 也就是 CONTRIBUTING 里描述的那个陷阱：把管线指向自己的语料，会静默地把真实录音暂存进提交

### 修复

以下问题全都在首个公开版本**发布之前**就被抓住，也就是说没有任何已发布版本包含过它们。之所以记录下来，是因为**这一类**错误恰恰是这个项目最该提防的。

- **`docs/ui.svg` 泄露了真实数据。** 第一个版本是照着这个项目来源的那个内部工作台画的，于是它带着 4 个真实姓名、一个真实班级名、源语料的规模，以及一句逐字摘自真实录音的话。现在它由自带 demo 生成（`make_demo.py`），这也让它与读者克隆后看到的东西一致。同一轮清理还去掉了 `index.html` 里两个拿真实姓名当示例的 placeholder、测试套件里的一个，以及后来两份 README 里一个用了真实姓氏、真实邮箱前缀和真实姓名全拼的拼音示例。
- **`start.sh` 没有可执行位**（`100644`）。Windows 没有可执行位，git 也不会自己推断一个出来，所以刚克隆到 Linux/macOS 的目录跑不了 `./start.sh` —— 而那正是 README 给出的命令。用 `git update-index --chmod=+x` 修好了。
- 文档里写的测试数量错过一次，又错了一次：先是 100，然后是 136，demo 那次改动又把它推到 140。数量会变是因为好几项检查依赖数据，所以文档不再写具体数字，CONTRIBUTING 解释了原因。

### 已记录的局限

- 单段音频几乎不携带身份信息（同人 0.579，对比最相似的异人 0.567 —— 分布重叠）
- 只有 1–2 段的说话人无法确认
- 音色相近的两个人无法区分；必须由人来听
- 内容相似不等于身份相同（两个组都"招待过同一个人"，实测 0.478）

### 备注

- 没有 `torch`、不下载模型、运行时不访问网络。嵌入向量的提取步骤在本仓库之外；接口见 `docs/METHOD.zh-CN.md` §1 与 §6。
- 自带 demo 是刻意合成的 —— 声纹属于生物识别数据，而真实的会议录音是私人内容。
